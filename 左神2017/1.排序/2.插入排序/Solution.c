#include <stdio.h>

int main(void){
    int arr[] = {5, 4, 3, 2, 0, 1};
    int n, i, j, tmp;
    n = sizeof(arr) / sizeof(arr[0]);

    for(i=0; i<n-1; i++){
        for (j=i+1; j<n-1; j++){

        }
    }

    return 0;
}